version: '3'

services:
  backend:
    build:
      dockerfile: Dockerfile
      context: ./recipesbook-service
    ports:
      - "8080:8080"
  frontend:
    build:
      dockerfile: Dockerfile
      context: ./recipesbook-client
    ports:
      - "4200:80"
    

