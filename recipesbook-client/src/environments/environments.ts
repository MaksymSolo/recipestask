export const environment = {
    production: false,
    apiBaseUrl: 'http://localhost:8080'
}

export const pagination = {
    defaultPage: 1,
    defaultSize: 4,
    defaultSort: "name",
    defaultDirection: "ASC"
}